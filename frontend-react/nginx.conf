# Nginx 配置文件
# 用于在 Docker 容器中提供前端静态文件服务

server {
    listen 80;
    server_name _;

    # 静态文件根目录
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由支持：所有请求都返回 index.html
    location / {
        try_files $uri /index.html;
    }
}
