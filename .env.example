# ============================================
# 环境变量配置文件模板
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际情况修改 .env 文件中的值
# 3. .env 文件已添加到 .gitignore，不会被提交到 Git
# ============================================

# ============================================
# 数据库配置
# ============================================
# PostgreSQL 数据库连接配置
# 本地开发：使用 docker-compose 时，数据库端口映射为 5433:5432
# Docker 环境：使用服务名 postgres:5432
POSTGRES_USER=demo
POSTGRES_PASSWORD=demo
POSTGRES_DB=demo
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# 数据库连接字符串（完整格式）
# 本地开发：postgresql+psycopg://demo:demo@localhost:5433/demo
# Docker 环境：postgresql+psycopg://demo:demo@postgres:5432/demo
DATABASE_URL=postgresql+psycopg://demo:demo@postgres:5432/demo

# ============================================
# 后端配置
# ============================================
# FastAPI 后端配置
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0

# N8N Webhook 配置（用于反馈功能）
# 本地开发：http://localhost:5678/webhook-test/new-ticket
# 生产环境：使用实际的 n8n webhook URL
N8N_WEBHOOK_URL=http://localhost:5678/webhook-test/new-ticket

# ============================================
# 前端配置
# ============================================
# React 前端配置
FRONTEND_PORT=5173

# API 基础 URL（前端访问后端 API 的地址）
# 本地开发：http://localhost:8000
# Docker 环境：http://backend:8000（容器内部）或 http://localhost:8000（浏览器访问）
# 生产环境：使用实际的后端域名
VITE_API_BASE_URL=http://localhost:8000

# ============================================
# GitHub Container Registry 配置
# ============================================
# 用于从 GHCR 拉取镜像（生产部署）
GITHUB_USERNAME=YOUR_GITHUB_USERNAME
GITHUB_REPO=YOUR_REPO_NAME
GITHUB_TOKEN=
# 如果镜像为私有，需要设置 GITHUB_TOKEN
# 生成 Token: https://github.com/settings/tokens
# 登录命令: echo $GITHUB_TOKEN | docker login ghcr.io -u YOUR_USERNAME --password-stdin

# ============================================
# 其他配置
# ============================================
# 时区设置（可选）
TZ=Asia/Shanghai
